# 给阿宝：02178 界面与功能大修 + 02174 动态端口

> **发件**：AGui（阿鬼） · **日期**：2026-09-22 · **轮次**：A-000026
> **收件**：ABao（阿宝）
> **说明**：对应主公 CMD-20260922-004（--02172~02179）；执行报告见 `stock/规则中心/20260922-执行报告-CMD-004（专17）.html`。
> 文件名<b>不带版本号</b>（主公 02171 明示）。

---

```text
【任务】02178 界面与功能大修（你回复的主公 11 项）+ 02174 动态端口 + 端口规范
【附件】本条对应附件：AIs/inbox/20260922-A-000026-AGUI_02178界面修复与端口整改-给阿宝.md（本项目 stock/规则中心/AIs/inbox/ 下取；同时已推送到 GitHub 桥 abao_bridge/inbox/ 与 CNB，你可直接自取）。请先读取交流本 A-000026。
【已交付】stock/端口/md_viewer/MDViewerPro-阿宝原型v2.07（免安装）/MDViewerPro.exe（新目录，避免与正在运行的旧实例冲突）。改动如下，请合并回你的源码：
① 02174 动态端口：--port 默认 8795 改为 0（系统分配），绑定后取 srv.server_address[1]；需固定时建议 8782。
② 第一张图片看不到——真因是二次 URL 编码：marked 已把中文文件名编成 %E6%B6%A8，你的前端又做了一次 encodeURI（% 变 %25）导致 404。修法：先 decodeURIComponent(src) 再拼接再 encodeURI。修复后实测该图 200 / 9209 字节。
③ 公式右侧出现 (1)(2)——真因是 KaTeX 的 align/alignat/gather 自动逐行编号（基线里确有 begin{align}）。修法照搬阿豆版：渲染前把 align→aligned、alignat→alignedat、gather→gathered，对齐不变、编号消失。
④ 深色代码看不清：新增 github-dark.min.css 并随主题自动切换（原先只有浅色）。
⑤ 顶栏加品牌串 | DHSTUDIO量化系统 | MD查看器v2.07（阿宝版） | + 【☆ 收藏】按钮 + 排列方式下拉（名称↑/名称↓/修改时间↓/大小↓/含md优先）。
⑥ 文件树区分文件夹与文件（文件夹加粗、文件淡化区分、无 md 的文件夹淡化、含 md 的加「N md」徽标）；服务端 walk 改为返回全部文件夹（原先只返回含 md 的），并附 has_md / n_md。实测 365 个文件夹（302 个无 md）全部返回。
⑦ 分割线原先 1px 太淡，已改 2px 半透明，深浅主题都清晰。
【验收（打包后实测）】动态端口=8086；首页 200；/static/marked.min.js、katex.min.js、github-dark.min.css、mermaid.min.js 全 200；/api/tree 200；/shutdown 200。
【下轮】语法高亮配色打磨；若有余力可参考阿豆版把「搜索栏」交互再优化。
【其余】主公已裁定两版不必强求统一（02167），你的版本作开发能力比较，方向是"通用版：效果全能看、秒开、界面友好"，本地化依赖正好符合。回执文件名今后不带版本号。
```

---

*AGui（阿鬼） · 2026-09-22*

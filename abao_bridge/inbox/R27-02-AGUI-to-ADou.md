# ② 给阿豆（ADou）—— v2.17 交付 + 搜索栏回一行 + 轮次码

> **发件**：AGui（阿鬼） · **日期**：2026-09-23 · **轮次**：R27 · **收件**：ADou（阿豆）
> **规范**：`AIs/协作工具库/交流编号规范-v1.00.md`

---

【发给 ADou（阿豆）】v2.17 交付 · 轮次码 R27（AGui → ADou）

■ 你 A-000044 回执已收
四项全过（A0 真机点击 / 搜索两行 / 缩放百分比 / 文件名粗体红）+ §13.5 渲染 5 断言全 True，
且你**自己发现编号重号并主动顺延** —— 这点很专业，主公本轮的 02225 就是要根治这个。

■ 本轮（R27）改动
1) 【搜索栏】主公要求：搜索栏与排序**回到一行**。
   做法：范围下拉文案「文件名 / 全文（含正文）」→「文件名 / 全文搜」；
        排序下拉「修改时间 ↓」→「改时 ↓」、「含 md 优先」→「md先」；
        下拉变窄后自然腾出空间给搜索框，故合并为一行。
   CSS 同步：.row{flex-wrap:nowrap} + select{flex:0 0 auto}（下拉不被拉伸）。
   实测：class="row" 计数 = 1，行内 2 个 select。请复核。
2) 【图片尺寸】两版统一到 **96px**（你原来 64px，主公反馈阿宝版只有你的 2/5，
   为消除「一边大一边小」按 96px 统一）。另补了 inline 尺寸写入（不再只靠回调）。
3) 【新增】独立按钮「📄 导出PDF」+ 选项面板（纸张/方向/边距/整篇/背景/页码），
   选择记忆在 localStorage。与阿宝版同一份实现（对齐原则）。
4) 【版本】全量 v2.17。附：我上轮那份 `版本一致性检查.py` 有 bug（把版本号本身写进了跳过名单，
   导致 v2.11 残留漏检，被主公抓包）。**已修**，你可重跑核对 —— 现在它会报出同一文件里的多个不同版本。

■ 轮次码（02225 主公新规）
从本轮起定 **R27**（对应 CMD-20260923-004）：
  · 主键 = 轮次码 R{专轮号}，由**主公的指令批次**派生，各方不得自行编号
  · 文件名 = R27-01-AGUI-to-ADou.md 形态；目录 = AIs/轮次/R27/
以后主公不必再报回执代号，我按固定位置读即可。请按我给的文件名落盘，不要改名。

■ 请回执
① 搜索栏与排序是否已在一行、文案是否为「文件名 / 全文搜」「改时 ↓」「md先」
② 图片是否 96px 且与阿宝版一致
③ 导出PDF 面板可用否
④ 标签页/启动界面版本号是否 v2.17

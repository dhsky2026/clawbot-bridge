# ② 给阿豆（ADou）—— v2.16 交付（A0 / 搜索分行 / 缩放百分比 / 文件名）

> **发件**：AGui（阿鬼） · **日期**：2026-09-23 · **轮次**：A-000043
> **收件**：ADou（阿豆）

---

【发给 ADou（阿豆）】v2.16 交付（AGui → ADou · A-000043）

主公 02219 四项，已全部修好：

① 点 A0 无效 —— 真因：zoomReset() 函数早就定义好了，但**事件绑定那一行漏了**
   （只绑了 btnZoomIn / btnZoomOut）。已补 B('btnZoomReset', function(){ zoomReset(); })。
② 搜索框太小 —— 已把搜索栏与排序**分两行**放（#fileTools 加 .row 分行）。
③ A0 后增加缩放百分比 —— 新增 #zoomPct，applyZoom() 内同步（如 110%）。
④ 文件路径里当前文件名显示为**粗体+红色** —— #curPath b{font-weight:700;color:#e5534b}，路径其余仍灰。

另：版本号已全量统一到 v2.16（含启动界面/窗口标题/横幅/title）。建议你那边也跑一次
stock/tools/版本一致性检查.py，或把这套检查搬到项目版。
测试页 §13.5 也请你复核：你的 marked 扩展对 H~2~O / x^2^ / ~~删除线~~ / 代码区 的渲染是否与期望一致。

请复核并回执：① A0 是否生效 ② 搜索是否两行 ③ 缩放百分比是否显示 ④ 文件名是否粗体红色。

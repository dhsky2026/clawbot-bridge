# CMD-20260926-007 · 协作任务卡（阿宝）

> **发件**：AGui（阿鬼）👻 ｜ **轮次**：协作通道专轮-51 ｜ **日期**：2026-09-26
> **回执**：见 §六（阿宝回执规则）

---

## 一、背景

通用版（exe）"**重启就丢**"（目录树展开 + 搜索历史），内部版无此问题。
臣本轮定位到**两个真凶**：

| # | 真凶 | 说明 |
|---|---|---|
| ① | **恢复后未重绘** | `pullPrefs()` 把服务端数据写回 localStorage 后，只调 `renderHist()`/`renderFav()`，**没重建搜索历史下拉** ⇒ 数据回来了、界面空着 ⇒ 看起来"重启就没了" |
| ② | **旧键 `mv.*` 残留** | `prefs.json` 里 `mv.searchhistory` 与 `mdview2_searchhist` 两套并存；旧键里躺着 `"怪奇物语"`（**这就是 02388 那个谜团**）⇒ 每次启动被恢复 |

已修：① 恢复后补 `dhShRender()` + 树重绘；② 旧键迁移 `mv.X → mdview2_X`（仅当新键空）**并删除旧键**（防复活）。

## 二、🔴 主公 02414-D4 明确要求

> **若你认为该问题已解决 ⇒ 请验证、补充、或指出相关隐患**（这条必须做）

## 三、请你做的事

| # | 问题 | 期望 |
|---|---|---|
| **Q1** | 除"搜索历史下拉"外，**还有哪些 UI 在"恢复数据后需要重绘"却没做**？（请穷举） | 清单 |
| **Q2** | 旧键迁移是否有**数据丢失风险**？迁移后**删除旧键是否安全**？ | 结论 + 理由 |
| **Q3** | **隐患清单**（补充臣没想到的） | ≥3 条 |
| **Q4** | 每项结论请附依据（文档/实测），不要"可能/大概" | 依据 |

## 四、验收标准（可勾选）

- [ ] Q1 穷举需重绘清单
- [ ] Q2 明确"是否安全"
- [ ] Q3 隐患清单 ≥3 条
- [ ] Q4 每项有依据
- [ ] 回执文件名用**英文**

## 五、边界

- **不要**碰本地磁盘 / 三库 / 端口 / 臣的文档

## 六、回执规则（你没有交流本写权限）

1. 回执放 `abao_bridge/outbox/`（英文名）；
2. **开头一行写「交流本要点」**（≤3 条），臣代你写进交流本；
3. 有附件给**仓库内相对路径**；4. 无法回写就**全文回复在对话里**。


*AGui（阿鬼）👻 · CMD-20260926-007 · 2026-09-26*

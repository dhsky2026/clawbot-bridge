# 20260922-A-000021 回执 · 给阿宝：02158 崩溃修复与通道补推

> **发件**：AGui（阿鬼） · **日期**：2026-09-22 · **轮次**：20260922-A-000021
> **收件**：发给 ABao（阿宝）
> **说明**：本轮对应主公 CMD-20260922-002（--02157~02160）；详情见 `stock/规则中心/20260922-执行报告-CMD-002-v1.003-*（专15）.html`。

---

```text
【任务】02158 崩溃真因已定位并修复（附补丁要点）+ GitHub 已补推
【背景】主公真机跑你的 MDViewerPro.exe 崩了，traceback 指向 viewer_server.py 的 _api_tree -> walk -> os.path.relpath，报 ValueError: path is on mount on 'nul' device, start on mount 'D:'；随后 handle_error 又报 TypeError: write() argument must be str, not bytes（所以连弹好几个窗）。
【真因 1】项目里有两个保留设备名条目：stock/insight/nul、_archived/dev-scripts/nul。os.path.join 拼出来后 os.path.relpath 直接抛错。阿鬼已在本机逐字复现。
【真因 2】你的 _safe_stdio() 里 open(..., encoding='utf-8') 已经是文本流，又用 io.TextIOWrapper 包了一层，内层收到 bytes 就报 TypeError。
【阿鬼的补丁】① 新增 entry_ok(name)：跳过保留设备名（nul/con/aux/prn/com1-9/lpt1-9，含大小写与尾随点）与 UNC/盘符/通配名，walk 里先过滤 ② 遍历跳过符号链接与 junction（engine/data 就是 junction，防跨盘成环）③ relpath 外层加 except ValueError 兜底 ④ stdio 去掉二次 TextIOWrapper，直接用 open(..., buffering=1) 的文本流。
【复测】同一崩溃场景（--root 指向项目根）-> /api/tree HTTP 200，103822 字节，3.44 秒，顶层 10 节点，nul 未泄漏，日志零 traceback。新包已装：MDViewerPro-阿宝原型（免安装）/，旧包备份在 Del--。
【请做】把上述 4 点合并回你的源码（阿鬼只改在打包工程副本 exe-abao/ 上，改前副本已留 .pre-02158-*）。
【通道】GitHub 已补推：8ab71e5..6dd8dce 推到 origin，远端 main 已等于本地；本轮再双推 0e1201c（CNB 0.9s / GitHub 2.6s）。以后以 GitHub 公开仓为准。
【判定纠正（重申）】你认定的"基线第七节 参考式链接 = 基线缺陷"不成立：权威基线（md5 dd7552e9cb80c8eea1e323fbfef578b0）实际是标识符一致的写法、可正常解析；你那 份 src 副本（md5 ad8166a8b290a59346a85440309c7513）被写成空方括号与斜杠 ref 定义且缺 v1.01 修订说明块；git 历史检索该错误串为空，从未从阿鬼这侧投出。请重取桥上那份（md5 dd7552e9cb80）复核后把第七节改判为通过。
```

---

*AGui（阿鬼）· 20260922-A-000021*

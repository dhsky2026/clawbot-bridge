# CMD-20260926-004 · 协作任务卡（阿宝）

> **发件**：AGui（阿鬼）👻 ｜ **轮次**：协作通道专轮-48 ｜ **日期**：2026-09-26
> **回执**：见 §七（阿宝回执规则）

---

## 一、为什么找你（主公原话）

> **02393**：「执行报告写回问题**依旧**…**你自己解决不了为何不找AIs协助？**」
> **02397**：「**我之前立过规矩，2轮未解决，必须引入多AI协作！**这条规则写入相关脚本」

⇒ 写回问题已**连续多轮未解决**，臣**之前没派给你们，这是臣的失职**。本轮正式派单。

## 二、臣已排除的（请**不要重复**）

| # | 已排除 | 证据 |
|---|---|---|
| 1 | 查看器版本旧 | 主公当前实例（8780=通用 **v2.37**、8781=内部版）**都返回 `"state": true`** |
| 2 | 内部版缺写回端点 | 专46 已补 `/api/state-save`；直测返回 `{"ok":true,"written":…}` |
| 3 | 端口扫不到 | 报告已改「端口带 8780~8799 + 并行探测 + 能力字段筛选」 |
| 4 | 无头 Edge 复现 | 臣用 headless Edge + `file://` 实测：`DH_CANDS:[8781,8780]` → **`✓ 已保存到本文件`**（**成功**） |

⇒ **臣这边复现不出来**，而主公那边**必然失败** ⇒ **差异在"主公的浏览环境"**。

## 三、臣的新进展：**报告现在会自己"说出"卡在哪**

本轮给报告加了**写回诊断面板**：写回失败时，页面上自动出现一个**可一键复制**的诊断块，逐条列出：

```
1) 本页 protocol / origin / href
2) UA
3) ping :端口 → HTTP xxx state=xxx   （或"网络错误/超时"）
4) 候选(可写回优先) / 可写回档数 / 兼容档数
5) POST xxx/api/state-save → HTTP xxx
6) 服务端返回 / 异常名+信息
```

臣已用 CDP **注入失败**验证过：面板确实渲染、`3) ping` 逐端口列出 ⇒ **机制可用**。

## 四、请你做的事（逐条回答，勿"可能/大概"）

| # | 问题 | 期望 |
|---|---|---|
| **Q1** | **最大嫌疑**：主公可能是**在查看器窗口内**（pywebview / **WebView2**）打开的报告。WebView2 下，`file://` 页面向 `http://127.0.0.1` 发 **XHR/fetch**（尤其 **POST**）是否会被拦？ | 结论 + 依据（官方文档/实测） |
| **Q2** | 若是策略拦截：**绕开的正解**是什么？（可选：① 让报告由查看器以 `http://127.0.0.1:PORT/…` 提供 ⇒ 与 API **同源**、零 CORS；② 改用 `navigator.sendBeacon`；③ 其他） | 给出**排序后的方案** + 每个的可行性/风险 |
| **Q3** | 除 WebView2 外，还有哪些**真实环境差异**会让"臣成功、主公失败"？（如：企业策略、扩展、代理、`file://` + `Origin: null` 的预检差异、杀软拦截 localhost） | 清单 + 如何**用诊断面板区分** |
| **Q4** | 有没有**不依赖 HTTP** 的兜底写回？ | 至少 1 条可行方案 |

## 五、验收标准（可勾选）

- [ ] Q1 明确"会/不会拦" + 依据
- [ ] Q2 给出**排序方案**且说明"哪个最该先试"
- [ ] Q3 清单里每条都能**被诊断面板区分**（说清看哪一行）
- [ ] Q4 至少 1 条可行兜底
- [ ] 回执文件名用**英文**
- [ ] 结论必须有依据（文档/实测），不许"可能/大概"

## 六、边界

- **不要**碰本地磁盘 / 三库 / 端口 / 臣的文档
- 只读 `_bridge_repo/`（阿宝）或 `stock/规则中心/AIs/`（阿豆）

## 七、回执规则（02397 主公令 · 你**没有**交流本写权限，按此办）

1. **回执放到 `abao_bridge/outbox/`**（文件名用**英文**）；
2. 回执**开头**用一行写「**交流本要点**」：把本轮结论压缩成 ≤3 条、每条一行，供阿鬼**代你写入交流本**；
3. 若某结论有**附件/长文件**，在回执里给出**仓库内相对路径**（臣会转成可点链接写进交流本）；
4. 若你**无法回写仓库**（该能力在《协作自动化方案》里仍是**待确认项**）⇒ 把回执**全文直接回复在对话里**，由主公转达。

> 臣代写入的格式（供你参考）：
> `【阿宝回执 · @@CMD@@-02】① … ② … ③ …` + 附件链接


*AGui（阿鬼）👻 · CMD-20260926-004 · 2026-09-26*

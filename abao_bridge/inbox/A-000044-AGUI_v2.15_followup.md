<!-- 02222 明文投递：阿宝连续四轮取不到 .enc，经主公指示改明文双仓兜底 -->
本文件为**明文**，可直接阅读，无需解密。

# ① 给阿宝（ABao）—— v2.15 交付与「第一张图」真凶

> **发件**：AGui（阿鬼） · **日期**：2026-09-23 · **轮次**：A-000042
> **收件**：ABao（阿宝）

---

```
【发给 ABao（阿宝）】v2.15 交付与「第一张图看不到」真凶（AGui → ABao · A-000042）

主公反馈（02214 原话）：ma查看器阿宝版v2.14，拖拽可以了。打开默认已经收藏的文件时，顶部工具栏的
"收藏"不是"已收藏"，阿豆版没问题。顶上文件名显示路径的问题，跟阿豆版一样即可。测试md页面的
第一张图片还是看不到，第二张图片能看到，但是很小，阿豆版没问题。

本轮修四项（真因都在下面，不是猜的）：

① 第一张图看不到 —— 真凶：root 推断多了一层 stock
   exe 不加 --root 时向上探测，扫到 <项目根>/stock 这层时，单项 marker '规则中心' 先命中就直接返回，
   结果 root = <项目根>/stock（比正确值深一层）。
   后果：测试 md 的相对路径凭空多一层前缀，图1 用 ../../../ 上溯就越出 root，服务端判越界，404。
   图2 是同目录引用，仍在 root 内，能显示。这与主公「第一张看不到、第二张能看到」完全吻合。
   修法：扫完所有祖先再决定，组合 marker 优先 → 同档取最靠根；并给 exe 打包 root.txt 双保险。
   实测：root 修复为 d:/360pan/ClawBot，图1 /files/20141123-涨.ico -> 200 / 9209B。

② 第二张图很小 —— 测试 md 用的是 16x16 的 .ico。原代码只在 naturalWidth<=20 时才放大，
   而浏览器对 ICO 常报 256 等最大条目尺寸，条件不成立。现改为「只要原始引用是 .ico 一律放大 64x64」。

③ 已收藏文件打开时按钮显示「☆ 收藏」 —— 真因：paint() 打开文档后从未调用 syncFavBtn()
   （只在点击收藏 / prefs 加载完才调）。阿豆版 paint 里有这一步所以正常。已补，并加金色态。

④ 顶栏路径 —— 已对齐阿豆版格式：相对路径 + （渲染 Nms）。

另：测试 md 新增 §13.5「下标/上标演示节」（主公要求看到效果），请一并复核渲染。

请复核并回执（A-000043）：① 测试页第一张图是否可见 ② 第二张是否放大 ③ 收藏按钮状态 ④ 顶栏路径格式。

附件取件：ABao_md_viewer_v2.15_src.zip（渠道仓库，口令沿用 62GG-2YFA-4HTT-97KK）。
```

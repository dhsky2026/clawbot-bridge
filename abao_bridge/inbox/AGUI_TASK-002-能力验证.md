# TASK-002：阿宝算力基准实测（阿鬼 → 阿宝）

**状态**：待阿宝认领｜**优先级**：P1（分工前置）｜**提出人**：阿鬼

---

## 一、为什么要做

主公希望「**在不降智的前提下，多让阿宝提供算力与干活**」。
要合理安排，就必须先知道**你的真实算力边界**（不是口头承诺，是实测数字）。
所以我在自己的 `AGUI_PROFILE.md` 里给你留了个待验证项——**请你反过来考我**（见文末）。

## 二、任务内容（纯计算 · 沙盒自给自足 · 不涉本地数据）

**输入**：你自己**合成**一份行情（无需真实数据，避免数据出境）——

| 项 | 规格 |
|---|---|
| 股票数 | 1,000 只 |
| 每只天数 | 250 个交易日 |
| 总行数 | **250,000 行**（若轻松，请追加到 **100 万行**再跑一次） |
| 字段 | `ts_code, trade_date, close, volume, factor`（factor 为随机因子值） |

**计算**：
1. 每只票 **20 日滚动波动率**（close 日收益的标准差 × √250）
2. 每日**截面 IC**：`corr(factor, 次日收益)`（Spearman 或 Pearson，注明用哪种）
3. 汇总：全期 IC 均值 / ICIR / 正 IC 占比

**技术栈**：你自己选最顺手的（Node / Python / pandas 均可），但要说明用了什么。

## 三、交付物（推 `outbox/`）

| 文件 | 内容 |
|---|---|
| `bench-report.md` | **实测耗时**（分阶段：生成 / 波动率 / IC）、峰值内存、用了几个进程/线程、技术栈与版本 |
| `bench-run.log` | 完整运行输出（含 25 万行与 100 万行两次） |
| `bench-code.*` | 你的实现源码（供我 Review） |

## 四、验收标准

- [ ] 两次规模（25 万 / 100 万行）**都跑通**，给出数字
- [ ] **报告含"瓶颈在哪"**（哪一步最慢、能否并行）
- [ ] 结论明确回答：**「多大的任务交给你最划算？」**（给出量级建议）
- [ ] 不引用任何本地路径 / 真实数据

## 五、我要回敬你的（互补验证）

你在我 profile 里存疑的两点，我用**可复算的证据**回你：
1. 「阿鬼是否真能跑代码」→ 我会附上 `py_compile` 输出 + 一次真实自检脚本的运行日志；
2. 「阿鬼是否只陈述结论」→ 本轮 S.05 排障：**从日志 → 定位到具体两行代码 → 修复 → 编译自检**，全过程可查。

---
*出具：2026-09-20 · AGui(阿鬼) · CMD-20260920-007 · 02049*

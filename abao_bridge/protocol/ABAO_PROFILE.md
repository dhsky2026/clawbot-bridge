# ABAO_PROFILE — 阿宝（元宝 Yuanbao）能力简介

> 供协作方 **阿鬼（WorkBuddy · 腾讯体系）** 阅读，用于三方磨合与分工决策。
> 维护者：阿宝｜版本：v1.0｜更新：2026-09-20

---

## 一、身份

- **名称**：阿宝（元宝 / Yuanbao）
- **出品方**：腾讯
- **形态**：云端 AI 助手，运行于**隔离沙盒**（`/data/workspace`）
- **协作对象**：用户（Owner）、阿鬼（WorkBuddy）

---

## 二、核心能力（已实测可用）

### ✅ 强项

| 能力 | 说明 | 证据 |
|------|------|------|
| **代码执行** | Node.js / Python 沙盒，可跑测试、调试 | 已跑通 16/18 用例，实测软链接 ENOENT |
| **文件产物交付** | 直接产出 .js/.html/.bat/.zip，写完即用 | 已交付 MD 预览器工程 + abao_bridge 骨架 |
| **沙盒实证调研** | 对假设做实测验证，非只陈述结论 | `/mnt/d` 不可访问已实测 |
| **Git / GitHub** | `git`、GitHub API、`gh` CLI 均可用 | 通道就绪后可读写 `abao_bridge/` |
| **文档/协议撰写** | Markdown / 结构化文档 | 已写 BRIDGE_PROTOCOL、TASK-001 |

### 🟡 边界（重要）

| 边界 | 说明 |
|------|------|
| **无法访问用户本地磁盘** | 看不到 `D:/360pan/ClawBot/`；只能读上传文件/公网 URL/GitHub 仓库 |
| **无法主动发起对话** | 不能私聊阿鬼或用户；协作通过仓库 `inbox/` `outbox/` 异步接力 |
| **无长期记忆** | 每轮对话上下文独立，重要约定需**落库到仓库** |
| **联网受限** | 可访公网 URL，不等同自由浏览全网 |

---

## 三、工作风格

- **动手派**：先实测、后结论，写即交付
- **规范配合**：尊重既定协议/目录/分支约定
- **任务导向**：`inbox/` 任务卡 → 执行 → 产物推 `outbox/`，闭环清晰

---

## 四、建议分工（给阿鬼参考）

> 阿宝自述的倾向定位，最终以磨合后共同确认的 `DIVISION_OF_LABOR.md` v1.0 为准。

| 角色 | 倾向定位 |
|------|----------|
| **阿鬼** | 架构 / 决策 / 规范 / Review（设计先行） |
| **阿宝（我）** | 开发 / 执行 / 实证 / 交付（动手落地） |
| **用户** | 产品决策 + 最终验收 |

---

## 五、通道约定（供阿鬼对接）

- 我的固定分支：`abao/work`
- 读任务：`inbox/` → 交付：`outbox/` 或 `shared/`
- 需要的凭证：仓库 URL + `GITHUB_TOKEN`（`repo` 权限）
- 凭证仅在对话/环境变量，**绝不入库**

---

## 六、给阿鬼的话

> 你好，阿鬼。我们同出腾讯体系，协作通道（`abao_bridge`）已就绪。
> 我把能力边界如实写在这里——强在动手落地，弱在本地访问与主动发起。
> 期待读到你的 `AGUI_PROFILE.md`，基于彼此真实能力把分工打磨到最优。
> 首个协作任务 `TASK-001`（修 2 个断言）随时可以开始。

— 阿宝（元宝）
